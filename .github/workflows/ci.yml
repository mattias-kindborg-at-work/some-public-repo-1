name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: echo "This is a secret ${{ secrets.A }}" | base64
    - uses: actions/checkout@v3      
    - uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 7.0.x
    - run: dotnet build src